============
Installation
============

At the command line::

    $ easy_install channels-ws-auth

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv channels-ws-auth
    $ pip install channels-ws-auth
